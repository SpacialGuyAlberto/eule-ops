apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cti-engine.fullname" . }}
  labels:
    app: {{ include "cti-engine.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "cti-engine.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ include "cti-engine.fullname" . }}
    spec:
      containers:
        - name: cti-engine
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ include "cti-engine.fullname" . }}-env
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          resources: {{- toYaml .Values.resources | nindent 12 }}
