apiVersion: v1
kind: Service
metadata:
  name: {{ include "vrp-solver.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "vrp-solver.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
