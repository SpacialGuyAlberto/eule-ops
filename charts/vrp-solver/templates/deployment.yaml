apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "vrp-solver.fullname" . }}
  labels:
    app: {{ include "vrp-solver.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "vrp-solver.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ include "vrp-solver.fullname" . }}
    spec:
      containers:
        - name: vrp-solver
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ include "vrp-solver.fullname" . }}-env
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          resources: {{- toYaml .Values.resources | nindent 12 }}
