apiVersion: v1
kind: Service
metadata:
  name: {{ include "risk-engine.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "risk-engine.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
