apiVersion: v1
kind: Service
metadata:
  name: {{ include "alert-service.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "alert-service.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
