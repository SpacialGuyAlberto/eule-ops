apiVersion: v1
kind: Service
metadata:
  name: {{ include "kanban.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "kanban.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
