spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/euleops
    username: postgres
    password: LeiserHazard1996!
    driver-class-name: org.postgresql.Driver
  flyway:
    locations: classpath:db/migration
    schemas: core,auth,cache
    defaultSchema: core


  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  kafka:
    bootstrap-servers: localhost:9093

  neo4j:
    uri: bolt://localhost:7687
    authentication:
      username: neo4j
      password: euleops4!
